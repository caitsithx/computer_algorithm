package leetcode.topeasy.array.intersect.unbalanced;

public class Solution {
   public int[] intersect(int[] small, int[] big) {
      //small in hashMap
      return new leetcode.topeasy.array.intersect.Solution().intersect(small, big);
   }
}
